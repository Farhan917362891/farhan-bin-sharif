#include <stdio.h>

int main() {
    int a[] = {10, 12, 15};
    
    printf("{\n");
    
    for (int i = 0; i < 3; i++) {
        printf("%d\n", a[i]);
        
        for (int j = 1; j < 3; j++) {
            printf("%d,%d\n",i,j);
        }
    }
    
    printf("}\n");
    
    return 0;
}
