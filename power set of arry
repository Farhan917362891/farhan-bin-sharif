#include <stdio.h>

int main() {
    int a[] = {10, 12, 15};
    int n = 3;

    int total = 1 << n; 

    for (int i = 0; i < total; i++) {
        printf("{ ");
        
        for (int j = 0; j < n; j++) {

            if (i & (1 << j)) {
                printf("%d ", a[j]);
            }
        }
        
        printf("}\n");
    }

    return 0;
}
